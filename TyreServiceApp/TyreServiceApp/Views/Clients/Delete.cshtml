@model TyreServiceApp.Models.Client

@{
    ViewData["Title"] = "Удалить клиента";
}

<div class="row justify-content-center">
    <div class="col-md-8 col-lg-6">
        <div class="card shadow-sm border-danger">
            <div class="card-header bg-danger text-white">
                <h5 class="mb-0"><i class="bi bi-exclamation-triangle"></i> Удаление клиента</h5>
            </div>
            <div class="card-body">
                <div class="alert alert-warning" role="alert">
                    <i class="bi bi-exclamation-triangle-fill"></i>
                    <strong>Внимание!</strong> Вы уверены, что хотите удалить этого клиента? Это действие нельзя отменить.
                </div>
                
                <dl class="row mb-0">
                    <dt class="col-sm-4 text-muted">
                        <i class="bi bi-person text-dark"></i> ФИО:
                    </dt>
                    <dd class="col-sm-8">
                        <strong>@Html.DisplayFor(model => model.FullName)</strong>
                    </dd>
                    
                    <dt class="col-sm-4 text-muted">
                        <i class="bi bi-telephone text-dark"></i> Телефон:
                    </dt>
                    <dd class="col-sm-8">
                        <strong>@Html.DisplayFor(model => model.Phone)</strong>
                    </dd>
                </dl>
            </div>
            <div class="card-footer bg-light">
                <form asp-action="Delete" class="d-flex gap-2">
                    <input type="hidden" asp-for="ClientId" />
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Удалить
                    </button>
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left"></i> Отмена
                    </a>
                </form>
            </div>
        </div>
    </div>
</div>
