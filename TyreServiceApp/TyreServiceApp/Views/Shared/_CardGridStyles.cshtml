<style>
        .tires-grid, .masters-grid, .works-grid, .services-grid, .orders-grid, .clients-grid, .cars-grid, .posts-grid, .consumables-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(var(--gm, 360px), var(--gu, 1fr)));
        gap: var(--gg, 16px);
    }

        .tire-card, .master-card, .work-card, .service-card, .order-card, .client-card, .car-card, .post-card, .consumable-card {
        font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
        background: #ffffff;
        border: 1.5px solid #a1a1a5;
        border-radius: 16px;
        padding: 18px 22px;
        display: flex;
        flex-direction: column;
        transition: border-color 0.2s ease;
    }
    .tire-card:hover, .master-card:hover, .work-card:hover, .service-card:hover, .order-card:hover, .client-card:hover, .car-card:hover, .post-card:hover, .consumable-card:hover {
        border-color: #000000;
    }

    .add-card {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        gap: 12px;
        border: 1.5px dashed #bbb !important;
        background: #fafafa !important;
        color: #888;
        font-size: 14px;
        font-weight: 500;
        transition: all 0.2s ease;
    }
    .add-card:hover {
        border-color: #000 !important;
        color: #000;
        background: #f0f0f0 !important;
    }
    .add-card svg {
        color: inherit;
    }

    @@media (max-width: 768px) {
    .tires-grid, .masters-grid, .works-grid, .services-grid, .orders-grid, .clients-grid, .cars-grid, .posts-grid, .consumables-grid {
            grid-template-columns: 1fr;
            gap: 12px;
        }
    .tire-card, .master-card, .work-card, .service-card, .order-card, .client-card, .car-card, .post-card, .consumable-card {
            padding: 14px 16px;
        }
    }
</style>
