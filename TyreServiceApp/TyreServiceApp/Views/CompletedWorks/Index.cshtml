@* Index.cshtml *@
@model IEnumerable<TyreServiceApp.Models.CompletedWork>

@{
    ViewData["Title"] = "Выполненные работы";
}

<div class="card shadow-sm mb-4">
        <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
        <h4 class="mb-0"><i class="bi bi-check-circle"></i> Список выполненных работ</h4>
        <a asp-action="Create" class="btn btn-light btn-sm">
            <i class="bi bi-plus-circle"></i> Добавить работу
        </a>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover table-striped table-bordered mb-0">
                <thead class="table-dark">
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.OrderNumber)</th>
                        <th>@Html.DisplayNameFor(model => model.ServiceCode)</th>
                        <th>@Html.DisplayNameFor(model => model.MasterId)</th>
                        <th>@Html.DisplayNameFor(model => model.WheelCount)</th>
                        <th>@Html.DisplayNameFor(model => model.CompletionTimeMin)</th>
                        <th>@Html.DisplayNameFor(model => model.WorkTotal)</th>
                        <th class="text-center" style="width: 180px;">Действия</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td class="align-middle">
                                <i class="bi bi-hash text-dark me-2"></i>
                                <strong>@Html.DisplayFor(modelItem => item.OrderNumber)</strong>
                            </td>
                            <td class="align-middle">
                                <span class="badge bg-info">@Html.DisplayFor(modelItem => item.ServiceCode)</span>
                            </td>
                            <td class="align-middle">
                                <i class="bi bi-person-badge text-secondary me-2"></i>
                                @Html.DisplayFor(modelItem => item.MasterId)
                            </td>
                            <td class="align-middle">
                                <i class="bi bi-circle text-warning me-2"></i>
                                @Html.DisplayFor(modelItem => item.WheelCount)
                            </td>
                            <td class="align-middle">
                                <i class="bi bi-clock text-info me-2"></i>
                                @Html.DisplayFor(modelItem => item.CompletionTimeMin) мин
                            </td>
                            <td class="align-middle">
                                <i class="bi bi-currency-exchange text-success me-2"></i>
                                <strong>@Html.DisplayFor(modelItem => item.WorkTotal) ₽</strong>
                            </td>
                            <td class="align-middle">
                                <div class="btn-group btn-group-sm" role="group">
                                    <a asp-action="Details" asp-route-id="@item.WorkId" 
                                       class="btn btn-outline-info" 
                                       title="Просмотр">
                                        <i class="bi bi-eye"></i>
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@item.WorkId" 
                                       class="btn btn-outline-primary" 
                                       title="Редактировать">
                                        <i class="bi bi-pencil"></i>
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@item.WorkId" 
                                       class="btn btn-outline-danger" 
                                       title="Удалить">
                                        <i class="bi bi-trash"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>